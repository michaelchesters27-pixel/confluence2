[build]
  command = "npm run build"
  publish = "public"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"
  NPM_CONFIG_REGISTRY = "https://registry.npmjs.org/"

[functions]
  node_bundler = "esbuild"

[functions."eve-confluence-scan"]
  schedule = "*/5 * * * *"
